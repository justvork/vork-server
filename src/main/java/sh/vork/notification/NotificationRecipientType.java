package sh.vork.notification;

/**
 * Declares whether a notification target is a Vork-internal recipient context
 * or an external participant context.
 */
public enum NotificationRecipientType {
    INTERNAL,
    EXTERNAL
}
